# OpenCart 4.1 + Inertia.js + Vue 3 + Tailwind CSS

Modern UI layer for **OpenCart 4.1** built with **Inertia.js**, **Vue 3**, and **Tailwind CSS**.
Keeps OpenCart’s PHP backend & routing, while delivering an SPA-like experience without a separate API.

## ✨ Features
- OpenCart 4.1 integration (PHP backend stays the source of truth)
- Inertia.js bridge (server-side routing + client-side rendering)
- Vue 3 pages/components
- Tailwind CSS styling (utility-first)
- SPA-like navigation, partial reloads, form helpers
- Ready structure for Admin and/or Catalog UI (depending on your setup)

## 🧱 Tech Stack
- **OpenCart** 4.1 (PHP)
- **Inertia.js**
- **Vue 3**
- **Tailwind CSS**
- (Optional) Vite / npm scripts (if included)

## 📦 Requirements
- PHP 8.1+ (recommended)
- OpenCart 4.1.x installed
- Node.js 18+ / npm (for building assets)

## 🚀 Installation (example)
1. Clone the repo into your OpenCart project (or copy the extension/module files).
2. Install JS deps and build assets:
   ```bash
   npm install
   npm run build
